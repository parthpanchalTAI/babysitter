issues
1. edit profile (selected dob visible in date picker);

//Social Login steps after firebase setup (google login, fb login)
->keytool -genkeypair -v -storetype PKCS12 -keystore my-upload-key.keystore -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000
-> cd android/cd app -> keytool -list -v -keystore rbscarebuyer.keystore -alias rbscarebuyer

fb Login
1 create app on fb developer account
2 install react-native-fbsdk-next
3 enable fb auth from firebase console
4 android native set up
5 app run
6 RN code for login and logout fb

changes

1) Availability calendar library 
2) Chat notification


flow of availability calendar:
1) save date using asyncstorage in api calling func
2) and pass the state in markdates props

